import React from 'react';
import { Link } from 'react-router-dom';

const ListOfDetailActionsToUser = props => {
  return (
    <Link className="btn btn-secondary" to="/">
      Go back
    </Link>
  );
};

export default ListOfDetailActionsToUser;
